<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>LIST APP</title>
    <link rel="stylesheet" href="/stylesheets/style_pg8.css">
    <!-- <script src="/send_url.js"></script> -->
  </head>
  <body>
    <header>
      <a class="header-logo" href="/">LIST APP</a>
    </header>
    <div class="container">
      <div class="container-header">
        <h1>買い物リスト</h1>
      </div>
      <div class="index-table-wrapper">
        <div class="table-head">
          <span class="id-column">ID</span>
          <span>買うもの</span>
        </div>
        <!-- %で囲った中にJSを埋め込み、定数itemsオブジェクトを定義 -->
        <% const items = [
          {id: 1, name: 'じゃがいも'},
          {id: 2, name: 'にんじん'},
          {id: 3, name: 'たまねぎ'}
        ]; %>
        <ul class="table-body">
          <span class="id-column">
            <!-- forEachを用いてitemsの要素を出力、%=で囲むとブラウザ上に表示できる -->
            <% items.forEach((item) => { %>
            <li>
              <span class="id-column">
                <!-- itemのidプロパティの値を表示 -->
                <%= item.id %>
              </span>
              <span class="name-column">
                <!-- itemのnameプロパティの値を表示 -->
                <%= item.name %>
              </span>
            </li>
            <% }); %>
        </ul>
      </div>
    </div>
  </body>
</html>